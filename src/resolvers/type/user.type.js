export default {
	// async perfis(user) {
	// 	// implementar (Assunto novo!)
	// },
	// async name() {
	// 	return 'www'
	// }
	/* async token({ token }) {
		return token === undefined ? 'Off' : token
	} */
}
